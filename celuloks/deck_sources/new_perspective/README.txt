Drop the images for the "A new perspective" deck into this folder
(any format: jpg, png, webp, avif... - any size or ratio), then run:

    /opt/homebrew/bin/python3 prepare_deck.py

The deck needs at least 36 images. A file named back.png (or back.jpg...)
here becomes this deck's card back.
